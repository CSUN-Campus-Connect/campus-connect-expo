import { HomeScreen } from '@/home/HomeScreen';

/** @deprecated Prefer `HomeScreen` from `@/home/HomeScreen`; kept for existing imports. */
export const DashboardScreen = HomeScreen;

export default HomeScreen;
